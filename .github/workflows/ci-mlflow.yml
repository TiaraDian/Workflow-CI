name: MLflow CI Training + Docker

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  mlflow-ci:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Check Env
      run: |
        python --version
        pip --version

    - name: Install dependencies
      run: |
        pip install mlflow pandas numpy scikit-learn scipy

    - name: Reset MLflow Env
      run: |
        unset MLFLOW_RUN_ID
        unset MLFLOW_EXPERIMENT_ID
        rm -rf mlruns

    - name: Run MLflow Project
      run: |
        mlflow run MLProject --env-manager=local

    - name: Get latest MLflow run_id
      run: |
        RUN_ID=$(sqlite3 MLProject/mlflow.db \
          "select run_uuid from runs order by start_time desc limit 1;")
        echo "RUN_ID=$RUN_ID" >> $GITHUB_ENV
        echo "Latest run_id: $RUN_ID"

    - name: Build Docker Model
      run: |
        mlflow models build-docker \
          --model-uri runs:/$RUN_ID/model \
          --name tiaradian/mlflow-heart-disease

    - name: Log in to Docker Hub
      run: |
        echo "${{ secrets.DOCKER_PASSWORD }}" | docker login \
        -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

    - name: Push Docker Image
      run: |
        docker push tiaradian/mlflow-heart-disease